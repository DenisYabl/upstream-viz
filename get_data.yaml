virtual_flow_meter:
  vfm: "readFile format=parquet path=upstream/vfm"

well:
  params: "readFile format=parquet path=upstream/well_omds_sample2"
  pump: "readFile format=parquet path=upstream/pump_properties"
